'use client';

// Import xterm CSS for terminal components
import '@/styles/xterm.css';

export function XTermStyles() {
  // This component just ensures the CSS is loaded
  // The actual import happens at the top of the file
  return null;
}
