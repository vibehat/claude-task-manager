import { IntegratedWebSocketExample } from '../../components/IntegratedWebSocketExample';

export default function WebSocketIntegratedPage() {
   return (
      <div className="min-h-screen bg-gray-50">
         <IntegratedWebSocketExample />
      </div>
   );
}
